import { PatternValidatorDirectiveDirective } from './pattern-validator-directive.directive';

describe('PatternValidatorDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new PatternValidatorDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
