import { SortSystemsDataPipe } from './sort-systems-data.pipe';

describe('SortSystemsDataPipe', () => {
  it('create an instance', () => {
    const pipe = new SortSystemsDataPipe();
    expect(pipe).toBeTruthy();
  });
});
