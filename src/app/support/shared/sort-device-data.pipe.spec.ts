import { SortDeviceDataPipe } from './sort-device-data.pipe';

describe('SortDeviceDataPipe', () => {
  it('create an instance', () => {
    const pipe = new SortDeviceDataPipe();
    expect(pipe).toBeTruthy();
  });
});
